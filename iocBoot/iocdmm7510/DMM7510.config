# Device IP
epicsEnvSet("DEVICE_IP", "${DEVICE_IP}")
epicsEnvSet("DEVICE_PORT", "5025")

# PV prefixes
epicsEnvSet("PDMM", "${EPICS_PV_AREA_PREFIX}")
epicsEnvSet("RDMM", "${EPICS_PV_DEVICE_PREFIX}")

# Port names
epicsEnvSet("PORT", "DMMPORT")
